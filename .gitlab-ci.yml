stages:
  - build

variables:
  Solution_Name: "DIV-SOUND-backend.sln"
  Test_Project_Path: UnitTest/UnitTest.csproj"


test:
  tags:
    - backend
  stage: build
  script:
    - ls DIV-SOUND-backend
    - pwd
    - dotnet test $Test_Project_Path
  only:
    - master
  image: mcr.microsoft.com/dotnet/nightly/sdk:7.0

build:
  tags:
    - backend
  # image: docker:20.10.16
  stage: build
  services:
    # - docker:20.10.16-dind
  script:
    - echo "$CI_REGISTRY_USER"
    - echo "$DOCKERHUB_URL"
    - echo "dckr_pat_OjU46U862sGLHCGEEYw6StC5zDo" | docker login $DOCKERHUB_URL -u "digitalroseuwu" --password-stdin
    # - docker login $DOCKERHUB_URL -u digitalroseuwu-p dckr_pat_OjU46U862sGLHCGEEYw6StC5zDo
    - docker build . -t $CI_REGISTRY/:latest -f ./DIV-SOUND-backend/Dockerfile
    - docker push $CI_REGISTRY/:latest
  only:
    - master
  needs:
    - test
